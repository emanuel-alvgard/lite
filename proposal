# Logging que with that consists of one string array per
pass. Each thread fills it's own log array and then each array
is executed in synch starting with syntax log.

#all variables are iterable. How will that work??