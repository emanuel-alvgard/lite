someData: [24] i8
someToAdd: [24] i8

:: initIntData:
    a: (i8)
    a <i>: i

shared dynamic result: i8 
:: add:
    a: ()
    b: ()
    result: a + b # when iteration is done "result" is allocated in memory.

try:
    initIntData(someData)
    initIntData(someToAdd)
    add(someData, someToAdd)
catch typeError: write("non valid type for operation")
catch: skip



